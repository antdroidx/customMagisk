name: Build ARMSX2 128MB Universal Android APK

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Push trigger disabled after starting the Rust target build."
